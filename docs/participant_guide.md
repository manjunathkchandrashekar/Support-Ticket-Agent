# Participant Guide

Install the requirements, set `PYTHONPATH` to the project root, and run `python -m src.main`. Add Markdown policy documents under `data/knowledge_base` and JSON tickets matching the `Ticket` schema under `data/tickets`.
